{
  "name": "wellmind-appstore-screenshots",
  "private": true,
  "version": "1.0.0",
  "type": "commonjs",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "appstore:build": "npm run build && node dist/index.js",
    "appstore:watch": "npm run build && node dist/index.js --watch"
  },
  "dependencies": {
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.6.3"
  }
}
